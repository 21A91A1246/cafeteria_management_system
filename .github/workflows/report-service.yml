name: Report Service CI

on:
  push:
    paths:
      - 'backend/report-service/**'
      - 'backend/pom.xml'
  pull_request:
    paths:
      - 'backend/report-service/**'
      - 'backend/pom.xml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build Report Service
      run: |
        mvn -pl backend/report-service -am clean package

    - name: Verify Generated JAR
      run: |
        ls -l backend/report-service/target
