name: Menu Service CI

on:
  push:
    paths:
      - 'backend/menu-service/**'
      - 'backend/pom.xml'
  pull_request:
    paths:
      - 'backend/menu-service/**'
      - 'backend/pom.xml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build and Test Menu Service
      run: |
        # Build the menu-service module using the parent maven context
        mvn -pl backend/menu-service -am clean test
